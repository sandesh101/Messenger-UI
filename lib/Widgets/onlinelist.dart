import 'package:flutter/material.dart';

class OnlineList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Online List'),
    );
  }
}
