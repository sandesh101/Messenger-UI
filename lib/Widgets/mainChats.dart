import 'package:flutter/material.dart';

class MainChats extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.only(top: 180.0),
      child: Container(
        child: Text('Main chat body'),
      ),
    );
  }
}
